programa meuPrograma
	declare a,b, c: number.
	declare x,y: text.
	declare d: double.
	inicio
		escreva("Hello World").
		escreva("Fim do programa").
		leia(a).
		leia(b).
		escreva(a).
		se (a > 5) entao
			escreva("maior que 5").
		senao
		    escreva("menor que 5").
		fimse
		
		se (3 != 5) entao
			escreva("diferente").
		senao
			escreva("igual").
		fimse
		
		escreva("oi agora vamos testar outro").
		se (b >= 0) entao
		   escreva("b positivo").
		senao
		   escreva("b negativo").
		fimse
		
		d := 3 + 5 / 2.
	fim
fimprog

			